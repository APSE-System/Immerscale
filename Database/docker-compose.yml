version: '3.1'

services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: aaaaa
      MYSQL_DATABASE: benis
      MYSQL_USER: benisuser
      MYSQL_PASSWORD: benispassword
    ports:
      - "3306:3306"